import React from "react";
import Button from "../Button";

const Nav = () => {
  return (
    <div className="h-[72px] p-3 w-full bg-darkBlue rounded-lg flex justify-end">
      <Button>Add feedback</Button>
    </div>
  );
};

export default Nav;
